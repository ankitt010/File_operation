import requests
# from werkzeug.wrappers import request
# import requests

# data = requests.get('http://127.0.0.1:8000/get')
# print(dir(data))
import threading
import concurrent.futures
x = threading.Thread(target=func(),args=(4,))
with open concurrent.futures(m)

def func():
    for i in range(10):
        yield i
x = func()
print(next(x))
print(next(x))
print(next(x))
print(next(x))
print(next(x))
print(next(x))

# DELETE FROM MyTable WHERE RowId NOT IN (SELECT MIN(RowId) 
# FROM MyTable GROUP BY Col1, Col2, Col3);

select min(rowid) from 